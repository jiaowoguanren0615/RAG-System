# Compliance Notes

- System is for internal knowledge lookup only, not clinical or regulatory decision-making.
- All Q&A should be auditable (user, time, citations).
- Only `active` documents with valid `effective_date` should be used by default.
- Archived documents must be labeled as obsolete in responses.
